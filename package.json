{
  "name": "dynamic-grid-workspaces",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build -w ynotsoft-dynamic-grid",
    "example": "npm run dev -w example"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "overrides": {
    "ajv": "^8.12.0",
    "ajv-draft-04": "^1.0.0"
  },
  "dependencies": {
    "@tiptap/extension-character-count": "^3.20.0"
  }
}
